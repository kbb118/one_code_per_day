#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define rep1(i, n) for (int i = 1; i < (int)(n); i++)

int main() {
    int a,b;
    cin >> a >> b;
    cout << (a+b)/2 << " " << (a-b)/2 << endl;

    return 0;
}
